Back to [Stage 02 – Security Hardening](02-hardening.md)

# 3. Fail2Ban – Brute Force Protection

Fail2Ban was configured to protect SSH from repeated failed login attempts.

```bash
sudo apt install fail2ban -y
```


## Custom jail configuration:

```bash
sudo nano /etc/fail2ban/jail.local

```
![fail2ban](/docs/assets/fail2ban.png)


